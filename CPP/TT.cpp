#include <bits/stdc++.h>
using namespace std;
int a[1];

bool snt(int n) {
    if (n <= 1) return false;
    for (int i = 2; i * i <= n; ++i) {
        if (n % i == 0) return false;
    }
    return true;
}

int main(){
    freopen("TT.INP", "r", stdin);
    freopen("TT.OUT", "w", stdin);

    for (int i; i < 1; i++){
        cin >> a[i];
        if
    }
}
